apiVersion: v1
kind: Service
metadata:
  name: echo-server-lb
  labels:
    app: echo-server
spec:
  type: LoadBalancer
  selector:
    app: echo-server
  ports:
    - name: http
      port: 80
      targetPort: http
